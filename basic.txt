📌 Commands Demonstration
1. File & Directory Management

pwd → Show current directory

ls -la → List files with permissions & size

cd / cd .. → Navigate directories

touch file1.txt → Create empty file

rm -rf file1.txt → Delete file

mv file1.txt newfile.txt → Rename file

cp newfile.txt copyfile.txt → Copy file

find f* → Find files starting with "f"

2. Viewing & Editing Files

cat application.log → Show content

more application.log / less application.log → View content page by page

head application.log → Show first 10 lines

tail application.log → Show last 10 lines

vim application.log / nano application.log → Edit file

3. Process & System Monitoring

ps → Show running processes

top / htop → Live process monitoring

kill <pid> → Kill process by ID

df -h → Show disk usage

du -sh folder/ → Show size of folder

uname -a → Show system details

4. Permissions & Ownership

chmod +w fold1 → Add write permission

chown user:group file1.txt → Change owner

5. Networking

ifconfig / ip a → Show network interfaces

netstat -tulnp → Show active connections

ping google.com → Test connectivity

traceroute google.com → Trace path to host

ssh user@hostname → Remote login

scp file1.txt user@host:/path → Copy file remotely

curl https://example.com / wget https://example.com → Download data

6. Archiving & Compression

tar -cvf archive.tar folder/ → Create archive

tar -xvf archive.tar → Extract archive

zip archive.zip file1 file2 → Compress files

unzip archive.zip → Extract zip

7. Search & Text Processing

grep "error" application.log → Search word in file

sed 's/error/issue/g' application.log → Replace text

awk '{print $1}' file.txt → Print first column

cut -d " " -f2 file.txt → Cut field 2

sort file.txt → Sort file

wc -l file.txt → Count lines

watch -n 1 ls -l → Monitor changes every second

8. Scheduling & Utilities

crontab -e → Schedule tasks

date → Show current date/time

man ls → Show manual

📜 Output Report Idea

You can run these commands in a Linux system and document output in a markdown/PDF format as a final project report.
